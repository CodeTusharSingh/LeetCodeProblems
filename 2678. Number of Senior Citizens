class Solution {
    public int countSeniors(String[] details) {
        int res = 0;
        for (String x : details) {
            String y = x.substring(11, 13);
            if (Integer.parseInt(y) > 60) {
                res++;
            }
        }
        return res;
    }
}
